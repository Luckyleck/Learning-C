#include <stdio.h>

int main()
{

    /*
    Unary, operates on a single operand
    Binary, operates on two operands

    Plus and Minus (Unary)
        + (positive)
        - (negative)
    Arithmetic (Binary)
        +
        -
        /
        *
        %
    Increment Decrement (Unary)
        ++
        --
    Assignement Operators (Binary)
        =
        +=
        -=
        /=
        *=
        %=
    Comparison (Binary)
        &&
        ||
        !
    Conditional (Ternary)
        ? :

    */
    return 0;
}